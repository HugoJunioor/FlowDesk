import AppLayout from "@/components/AppLayout";

const Configuracoes = () => {
  return (
    <AppLayout>
      <div>
        <h1 className="text-2xl font-semibold text-foreground">Configurações</h1>
        <p className="text-muted-foreground text-sm mt-1">Ajustes do sistema</p>
        <div className="mt-8 text-sm text-muted-foreground">Em breve...</div>
      </div>
    </AppLayout>
  );
};

export default Configuracoes;
